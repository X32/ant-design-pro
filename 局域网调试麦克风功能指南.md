# 局域网调试麦克风功能指南

## 问题说明

现代浏览器出于安全考虑，麦克风权限只允许在以下环境中使用：
- ✅ **HTTPS** 环境
- ✅ **localhost / 127.0.0.1**
- ❌ **HTTP + 局域网 IP**（如 `http://192.168.1.100:8001`）

## 解决方案

### 方案一：使用 HTTPS 开发服务器（推荐）

#### 1. 启动 HTTPS 开发服务器

```bash
npm run start:https
```

这个命令会：
- 启动带 HTTPS 的开发服务器
- 自动生成自签名 SSL 证书
- 允许局域网访问（host: 0.0.0.0）

#### 2. 查看你的局域网 IP

**Mac/Linux：**
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
```

**Windows：**
```bash
ipconfig
```

找到类似 `192.168.x.x` 或 `10.x.x.x` 的 IP 地址。

#### 3. 在其他设备上访问

```
https://你的IP:8001
```

例如：`https://192.168.1.100:8001`

#### 4. 信任自签名证书

首次访问会提示证书不受信任，这是正常的：

**Chrome/Edge：**
1. 看到"您的连接不是私密连接"
2. 点击"高级"
3. 点击"继续前往 xxx.xxx.xxx.xxx（不安全）"

**Safari：**
1. 看到"此连接不是私密连接"
2. 点击"显示详细信息"
3. 点击"访问此网站"
4. 输入系统密码确认

**Firefox：**
1. 看到"警告：潜在的安全风险"
2. 点击"高级"
3. 点击"接受风险并继续"

**移动端（iOS/Android）：**
1. 看到证书警告
2. 点击"高级"或"详细信息"
3. 点击"继续"或"访问此网站"

---

### 方案二：使用 localhost 转发（适合简单测试）

如果只是想快速测试，可以在本机访问：

```bash
npm run start:dev
```

然后访问：`http://localhost:8001`

但这样无法在其他设备上测试。

---

### 方案三：配置受信任的证书（高级）

如果需要长期开发，可以配置受信任的证书。

#### 1. 生成本地 CA 证书（使用 mkcert）

```bash
# 安装 mkcert
# Mac
brew install mkcert
brew install nss # for Firefox

# Windows
choco install mkcert

# Linux
# 参考 https://github.com/FiloSottile/mkcert#installation

# 创建本地 CA
mkcert -install

# 生成证书（在项目根目录）
mkcert localhost 192.168.1.100 # 替换为你的实际 IP
```

#### 2. 修改配置使用证书

在 `config/config.ts` 中：

```typescript
https: process.env.HTTPS === 'true' ? {
  key: './localhost+1-key.pem',  // 替换为实际路径
  cert: './localhost+1.pem',     // 替换为实际路径
} : undefined,
```

#### 3. 启动服务

```bash
npm run start:https
```

---

## 常见问题

### Q1: 启动时报错 "certificate not found"

**原因**：没有找到 SSL 证书

**解决**：
- UmiJS 会自动生成自签名证书
- 如果报错，删除 `.cert` 文件夹后重试
- 或按照方案三手动生成证书

### Q2: 移动端无法访问

**原因**：
1. 防火墙阻止了 8001 端口
2. 移动设备和电脑不在同一网络

**解决**：
1. 关闭防火墙或开放 8001 端口
   ```bash
   # Mac 临时关闭防火墙
   sudo /usr/libexec/ApplicationFirewall/socketfilterfw --setglobalstate off
   ```

2. 确保在同一 WiFi 网络下

### Q3: 证书警告能否消除？

**开发环境**：
- 自签名证书会有警告，这是正常的
- 按照方案三使用 mkcert 可以避免警告

**生产环境**：
- 必须使用正式的 SSL 证书（Let's Encrypt 等）
- 不会有证书警告

### Q4: 已经使用 HTTPS，但还是报错？

**检查清单**：
1. 确认访问地址是 `https://`（不是 `http://`）
2. 确认已信任证书（点击了"继续访问"）
3. 清除浏览器缓存并刷新
4. 查看浏览器控制台的具体错误

### Q5: localhost 能用，局域网不能用？

**原因**：浏览器只允许 localhost 使用 HTTP + 麦克风

**解决**：必须使用 HTTPS 或方案三配置受信任证书

---

## 快速开始

### 1. 最简单的方式（推荐）

```bash
# 1. 启动 HTTPS 服务
npm run start:https

# 2. 查看你的 IP（Mac/Linux）
ifconfig | grep "inet " | grep -v 127.0.0.1

# 3. 在其他设备访问
# https://你的IP:8001

# 4. 信任证书（点击"继续访问"）
```

### 2. 如果不需要局域网测试

```bash
# 直接本地访问
npm run start:dev

# 访问 http://localhost:8001
```

---

## 开发建议

1. **本机开发**：使用 `npm run start:dev`（HTTP）
2. **测试麦克风功能**：使用 `npm run start:https`（HTTPS）
3. **移动端测试**：使用 `npm run start:https` + 局域网 IP
4. **生产部署**：必须配置正式的 SSL 证书

---

## 技术说明

### 为什么需要 HTTPS？

1. **安全性**：防止麦克风数据被中间人窃取
2. **隐私保护**：防止恶意网站偷偷录音
3. **浏览器策略**：Chrome、Safari、Firefox 都强制要求 HTTPS

### 支持的浏览器

| 浏览器 | HTTP + localhost | HTTP + 局域网 | HTTPS |
|--------|-----------------|--------------|-------|
| Chrome | ✅ | ❌ | ✅ |
| Safari | ✅ | ❌ | ✅ |
| Firefox | ✅ | ❌ | ✅ |
| Edge | ✅ | ❌ | ✅ |

---

## 相关文档

- [Web API: MediaDevices.getUserMedia()](https://developer.mozilla.org/zh-CN/docs/Web/API/MediaDevices/getUserMedia)
- [UmiJS HTTPS 配置](https://umijs.org/docs/api/config#https)
- [mkcert - 本地 HTTPS 开发工具](https://github.com/FiloSottile/mkcert)

---

**问题反馈**：如果遇到其他问题，请查看浏览器控制台（F12）的错误信息。
