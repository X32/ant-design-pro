# Ant Design Pro 项目宝塔部署指南

> 腾讯云 CentOS + 宝塔面板部署方案

## 📋 目录

1. [准备工作](#准备工作)
2. [服务器环境配置](#服务器环境配置)
3. [项目构建](#项目构建)
4. [上传部署](#上传部署)
5. [Nginx 配置](#nginx-配置)
6. [常见问题](#常见问题)

---

## 1️⃣ 准备工作

### 1.1 确认服务器信息

- **服务器系统**：CentOS
- **面板**：宝塔面板（已安装）
- **访问地址**：你的服务器 IP 或域名
- **宝塔面板地址**：`http://你的IP:8888`

### 1.2 确认后端 API 地址

假设你的后端 API 部署在：
- **开发环境**：`http://localhost:9003`
- **生产环境**：`https://api.yourdomain.com` 或 `http://你的IP:9003`

---

## 2️⃣ 服务器环境配置

### 2.1 登录宝塔面板

访问 `http://你的服务器IP:8888`，使用账号密码登录。

### 2.2 安装必要软件

在宝塔面板左侧菜单 **"软件商店"** 中安装：

#### ✅ 必装软件

1. **Nginx**（推荐 1.20+）
   - 用途：作为 Web 服务器，托管前端静态文件
   - 安装：点击"一键安装"

2. **PM2 管理器**（可选，如果需要部署 Node.js 后端）
   - 用途：管理 Node.js 进程
   - 安装：在"软件商店"搜索"PM2"安装

### 2.3 创建网站

1. 点击左侧菜单 **"网站"**
2. 点击 **"添加站点"**
3. 填写信息：
   ```
   域名：yourdomain.com（或填写服务器 IP）
   根目录：/www/wwwroot/myapp（自定义路径）
   FTP：不创建
   数据库：不创建
   PHP 版本：纯静态
   ```
4. 点击 **"提交"**

### 2.4 配置 HTTPS（推荐）

如果有域名，建议配置 HTTPS：

1. 在网站列表中找到刚创建的网站
2. 点击 **"设置"** → **"SSL"**
3. 选择 **"Let's Encrypt"**（免费证书）
4. 输入邮箱，点击 **"申请"**
5. 申请成功后，开启 **"强制 HTTPS"**

---

## 3️⃣ 项目构建

### 3.1 修改 API 地址

在本地项目中，修改 `src/config/apiConfig.ts`：

```typescript
// API基础地址
export const API_BASE_URL = isDev 
  ? '' 
  : 'https://api.yourdomain.com';  // 改为你的实际后端地址
  
// 如果前后端部署在同一服务器，可以使用：
// export const API_BASE_URL = isDev ? '' : '';  // 通过 Nginx 代理
```

### 3.2 本地构建项目

在项目根目录执行：

```bash
# 安装依赖（如果还没安装）
npm install

# 构建生产版本
npm run build
```

构建完成后，会在项目根目录生成 `dist` 文件夹，这就是需要上传到服务器的文件。

### 3.3 检查构建结果

```bash
# 查看 dist 目录结构
ls -la dist/

# 应该包含：
# - index.html（首页）
# - umi.js（编译后的 JS）
# - umi.css（编译后的 CSS）
# - 其他静态资源
```

---

## 4️⃣ 上传部署

### 方式一：宝塔面板在线上传（推荐新手）

1. **进入文件管理**
   - 点击宝塔面板左侧 **"文件"**
   - 进入网站根目录：`/www/wwwroot/myapp`

2. **清空目录**
   - 删除目录中的所有文件（包括默认的 index.html）

3. **上传文件**
   - 点击 **"上传"**
   - 选择本地项目的 `dist` 文件夹中的**所有文件**
   - 等待上传完成

4. **解压（如果打包上传）**
   - 如果文件太多，可以先在本地压缩 dist 文件夹为 `dist.zip`
   - 上传 `dist.zip` 到服务器
   - 右键点击 `dist.zip` → **"解压"**
   - 解压后删除 `dist.zip`

### 方式二：FTP 工具上传（推荐）

#### 4.1 创建 FTP 账户

1. 在宝塔面板点击 **"FTP"**
2. 点击 **"添加 FTP"**
3. 填写信息：
   ```
   用户名：myapp_ftp
   密码：（自动生成或自定义）
   根目录：/www/wwwroot/myapp
   ```
4. 点击 **"提交"**

#### 4.2 使用 FTP 工具上传

推荐工具：
- **Windows**：FileZilla、WinSCP
- **Mac**：FileZilla、Transmit

连接信息：
```
主机：你的服务器 IP
端口：21
用户名：myapp_ftp
密码：刚才设置的密码
```

上传步骤：
1. 连接成功后，右侧显示服务器目录
2. 删除服务器上的所有文件
3. 将本地 `dist` 文件夹中的**所有文件**拖到右侧上传

### 方式三：命令行上传（推荐熟练用户）

在本地项目根目录执行：

```bash
# 使用 scp 上传（需要先配置 SSH 密钥）
scp -r dist/* root@你的服务器IP:/www/wwwroot/myapp/

# 或使用 rsync（推荐，支持增量上传）
rsync -avz --delete dist/ root@你的服务器IP:/www/wwwroot/myapp/
```

---

## 5️⃣ Nginx 配置

### 5.1 配置前端路由支持

由于 Ant Design Pro 使用前端路由（React Router），需要配置 Nginx 将所有路由指向 `index.html`。

#### 方法一：宝塔面板配置（推荐）

1. 在网站列表中找到你的网站
2. 点击 **"设置"** → **"配置文件"**
3. 找到 `location /` 部分，修改为：

```nginx
location / {
    try_files $uri $uri/ /index.html;
    
    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}
```

4. 点击 **"保存"**

### 5.2 配置静态资源缓存

在配置文件中添加（在 `location /` 后面）：

```nginx
# 图片缓存 1 年
location ~* \.(jpg|jpeg|png|gif|ico|svg)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
}

# CSS 和 JS 缓存 1 年
location ~* \.(css|js)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
}

# 字体文件缓存 1 年
location ~* \.(woff|woff2|ttf|eot)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
}
```

### 5.3 配置 API 反向代理（可选）

如果前后端部署在同一服务器，可以通过 Nginx 代理后端 API：

1. 点击网站 **"设置"** → **"反向代理"**
2. 点击 **"添加反向代理"**
3. 填写信息：
   ```
   代理名称：API代理
   目标URL：http://127.0.0.1:9003
   发送域名：$host
   ```
4. 在 **"配置文件"** 中添加：

```nginx
# API 反向代理
location /api/ {
    proxy_pass http://127.0.0.1:9003;
    
    # 代理头配置
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    
    # 超时配置
    proxy_connect_timeout 60s;
    proxy_send_timeout 60s;
    proxy_read_timeout 60s;
    
    # 请求体大小限制（文件上传）
    client_max_body_size 100M;
}
```

### 5.4 开启 Gzip 压缩

1. 点击网站 **"设置"** → **"配置文件"**
2. 确保有以下配置：

```nginx
# Gzip 压缩
gzip on;
gzip_vary on;
gzip_min_length 1024;
gzip_comp_level 6;
gzip_types
    text/plain
    text/css
    text/xml
    text/javascript
    application/json
    application/javascript
    application/x-javascript
    application/xml
    application/xml+rss
    application/atom+xml
    image/svg+xml;
```

### 5.5 重启 Nginx

配置修改后，需要重启 Nginx：

1. 点击左侧 **"软件商店"**
2. 找到 **"Nginx"**
3. 点击 **"重载配置"** 或 **"重启"**

---

## 6️⃣ 验证部署

### 6.1 访问网站

在浏览器中访问：
- **HTTP**：`http://你的域名` 或 `http://你的IP`
- **HTTPS**：`https://你的域名`

### 6.2 检查清单

- ✅ 首页能正常访问
- ✅ 刷新页面不会出现 404
- ✅ 前端路由跳转正常
- ✅ 静态资源（图片、CSS、JS）加载正常
- ✅ API 请求能正常访问后端
- ✅ 麦克风权限功能正常（需要 HTTPS）

### 6.3 测试 API 连接

打开浏览器控制台（F12）：

1. 查看 **Network** 标签
2. 尝试登录或其他需要 API 的操作
3. 检查 API 请求是否成功
4. 如果失败，查看错误信息

---

## 7️⃣ 常见问题

### 问题 1：刷新页面出现 404

**原因**：Nginx 未配置前端路由支持

**解决**：
1. 检查 Nginx 配置中是否有 `try_files $uri $uri/ /index.html;`
2. 如果没有，按照 [5.1 配置前端路由支持](#51-配置前端路由支持) 操作
3. 重载 Nginx 配置

### 问题 2：API 请求失败（CORS 错误）

**原因**：跨域问题

**解决方案一**：后端配置 CORS
```javascript
// 后端添加 CORS 头
app.use(cors({
  origin: 'https://yourdomain.com',  // 前端域名
  credentials: true
}));
```

**解决方案二**：使用 Nginx 反向代理
- 按照 [5.3 配置 API 反向代理](#53-配置-api-反向代理可选) 操作
- 前端 API 地址改为空字符串：`export const API_BASE_URL = '';`

### 问题 3：麦克风权限被拒绝

**原因**：非 HTTPS 环境下浏览器限制麦克风权限

**解决**：
1. 配置 HTTPS 证书（参考 [2.4 配置 HTTPS](#24-配置-https推荐)）
2. 或在本地测试时使用 `localhost`

### 问题 4：静态资源 404

**原因**：文件路径不对或上传不完整

**解决**：
1. 检查服务器目录 `/www/wwwroot/myapp` 是否有所有文件
2. 确保 `index.html` 在根目录
3. 检查文件权限：`chmod -R 755 /www/wwwroot/myapp`

### 问题 5：上传文件过大失败

**原因**：Nginx 限制请求体大小

**解决**：
1. 在 Nginx 配置中添加：
   ```nginx
   client_max_body_size 100M;
   ```
2. 重载 Nginx 配置

### 问题 6：部署后页面空白

**原因**：
- 资源路径不对
- 构建失败
- API 地址配置错误

**解决**：
1. 打开浏览器控制台（F12）查看错误
2. 检查 Network 标签是否有资源加载失败
3. 检查 Console 标签是否有 JS 错误
4. 检查 `src/config/apiConfig.ts` 中的 API 地址是否正确

---

## 8️⃣ 性能优化建议

### 8.1 启用 HTTP/2

在宝塔面板中：
1. 网站设置 → SSL → 开启 **"HTTP/2"**

### 8.2 配置 CDN（可选）

如果网站访问量大，可以使用腾讯云 CDN：
1. 在腾讯云控制台开通 CDN
2. 添加加速域名
3. 配置源站地址为你的服务器 IP
4. 在宝塔面板配置 DNS 解析到 CDN

### 8.3 开启浏览器缓存

已在 [5.2 配置静态资源缓存](#52-配置静态资源缓存) 中配置

---

## 9️⃣ 更新部署流程

每次更新代码后：

```bash
# 1. 拉取最新代码
git pull

# 2. 安装新依赖（如果有）
npm install

# 3. 构建
npm run build

# 4. 上传到服务器
# 使用 FTP 或 scp 上传 dist 文件夹内容

# 5. 清除浏览器缓存
# 按 Ctrl+F5 强制刷新
```

---

## 🎯 快速部署脚本（可选）

创建一个 `deploy.sh` 脚本，实现一键部署：

```bash
#!/bin/bash

echo "开始构建..."
npm run build

echo "上传到服务器..."
rsync -avz --delete dist/ root@你的服务器IP:/www/wwwroot/myapp/

echo "部署完成！"
```

使用方法：
```bash
chmod +x deploy.sh
./deploy.sh
```

---

## 📞 需要帮助？

如果遇到问题，请检查：
1. 宝塔面板日志：左侧菜单 → **"日志"**
2. Nginx 错误日志：`/www/wwwroot/myapp/logs/`
3. 浏览器控制台（F12）

---

## ✅ 部署完成检查清单

- [ ] 服务器环境配置完成（Nginx 已安装）
- [ ] 网站已创建并配置 HTTPS
- [ ] 本地项目构建成功（dist 文件夹已生成）
- [ ] 文件已上传到服务器
- [ ] Nginx 配置已修改（支持前端路由）
- [ ] API 代理配置完成（如果需要）
- [ ] 网站可以正常访问
- [ ] 刷新页面不会 404
- [ ] API 请求正常
- [ ] 麦克风功能正常（HTTPS 环境）

---

**祝部署顺利！** 🎉
